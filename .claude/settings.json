{
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Write|Edit",
        "hooks": [
          {
            "type": "command",
            "command": "jq -r '.tool_input.file_path // empty' | { read -r f; [[ \"$f\" == *.ts || \"$f\" == *.tsx ]] && pnpm eslint --fix \"$f\"; } 2>/dev/null || true",
            "timeout": 30,
            "statusMessage": "ESLint auto-fix..."
          }
        ]
      }
    ]
  }
}
